import { Box, Typography } from "@mui/material";
import React from "react";

export default function Footer() {
  return (
    <Box id="footer" sx={{ height: "100vh", backgroundColor: "crimson" }}>
      <Typography align="center">Footer</Typography>
    </Box>
  );
}
